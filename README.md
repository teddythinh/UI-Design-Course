# UI Design Course

## Instruction

Install dependencies

```bash
npm install
```

Run the project

~~npm run start:dev~~

The above command is being buggy, so use the following command instead:

```bash
npm start
```
